require 'spec_helper'

describe SimpleActiveModelValidators do
  it 'has a version number' do
    expect(SimpleActiveModelValidators::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
